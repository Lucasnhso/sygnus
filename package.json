{
  "name": "sygnus",
  "version": "0.0.2",
  "description": "sygnus",
  "private": false,
  "types": "build/types/types.d.ts",
  "prisma": {
    "schema": "example/database/prisma/schema.prisma",
    "seed": "tsx ./src/database/prisma/seeds/index.ts"
  },
  "scripts": {
    "dev": "tsx watch ./example/server.ts",
    "clean-build": "rimraf -rf ./build",
    "compile": "tsc -p .",
    "copy-templates": "copyfiles ./src/templates/* ./build/templates",
    "build": "yarn compile "
  },
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.5.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.15",
    "@types/express": "^4.17.20",
    "@types/node": "^12.7.11",
    "prisma": "^5.5.2",
    "tsx": "^3.14.0",
    "typescript": "5.1.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
